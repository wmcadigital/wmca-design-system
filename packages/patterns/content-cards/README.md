# Content Cards (pattern package)

This package contains the Content Cards pattern templates and example fragments.

Install and run the build to copy `src/` into `dist/`:

```
npm --prefix packages/patterns/content-cards run build
```
