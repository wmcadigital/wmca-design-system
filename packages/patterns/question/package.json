{
  "name": "@wmcadigital/patterns/question",
  "version": "1.0.0",
  "description": "Question patterns (confirmation, form-element, reference, summary)",
  "main": "dist",
  "files": ["dist"],
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && rsync -a src/ dist/ && node ../../../scripts/render-packages-njk.js dist"
  },
  "author": "WMCA Digital",
  "license": "MIT"
}
