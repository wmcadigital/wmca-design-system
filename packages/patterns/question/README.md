# Question patterns

This package contains several question-related patterns used across WMCA sites (confirmation-message, form-element, reference, summary).

Build: `npm run build` from within the package folder.

Outputs are placed in `dist/` and include rendered HTML examples and the Nunjucks source.
