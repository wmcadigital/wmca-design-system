# Banner pattern

This package contains the Banner pattern styles and templates used by the WMCA design system.

Files:

- `dist/` - distributable SCSS and Nunjucks templates
- `src/` - source SCSS and Nunjucks templates

Build:

```bash
npm run build
```
