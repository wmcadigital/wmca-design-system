# Branded banner pattern

This package contains the Branded banner pattern styles and template used by the WMCA design system.

Files:

- `dist/` - distributable SCSS and Nunjucks template
- `src/` - source SCSS and Nunjucks template

Build:

```bash
npm run build
```
