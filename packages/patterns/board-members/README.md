# Board members pattern

This package contains the Board members pattern styles, templates and example JS used by the WMCA design system.

Files:

- `dist/` - distributable SCSS, Nunjucks templates and JS
- `src/` - source SCSS, Nunjucks templates and example JS

Build:

```bash
npm run build
```
