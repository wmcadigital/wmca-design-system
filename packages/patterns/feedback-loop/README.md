# Feedback Loop (pattern package)

This package contains the Feedback Loop pattern templates, styles and example JS.

Build (copies `src/` → `dist/`):

```bash
npm --prefix packages/patterns/feedback-loop run build
ls -R packages/patterns/feedback-loop/dist
```
