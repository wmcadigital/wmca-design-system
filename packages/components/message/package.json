{
  "name": "@wmcadigital/components/message",
  "version": "0.1.0",
  "description": "Message components (summary, help) from WMCA design system",
  "main": "dist/message.min.css",
  "style": "dist/message.min.css",
  "files": ["dist","src"],
  "scripts": {
    "build:css": "sass src/_package.scss dist/message.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:njk": "mkdir -p dist && cp src/*/_*.njk dist/ || true",
    "build": "npm run build:css && npm run build:njk",
    "prepare": "npm run build"
  },
  "keywords": ["wmca","design-system","message","css","nunjucks"],
  "author": "WMCA",
  "license": "MIT",
  "publishConfig": {"access":"public"},
  "devDependencies": {"sass":"^1.32.0"}
}
