{
  "name": "@wmcadigital/components/link",
  "version": "0.1.0",
  "description": "Link component styles and Nunjucks macro from WMCA design system",
  "main": "dist/link.min.css",
  "style": "dist/link.min.css",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build:css": "sass src/_package.scss dist/link.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:njk": "mkdir -p dist && cp src/link/_link.njk dist/link.njk || true",
    "build": "npm run build:css && npm run build:njk",
    "prepare": "npm run build"
  },
  "keywords": ["wmca", "design-system", "link", "css", "nunjucks"],
  "author": "WMCA",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "sass": "^1.32.0"
  }
}
