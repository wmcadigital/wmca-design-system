{
  "name": "@wmcadigital/components/video-embed",
  "version": "0.1.0",
  "description": "Video embed component (SCSS and Nunjucks) from WMCA design system",
  "main": "dist/video-embed.min.css",
  "style": "dist/video-embed.min.css",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build:css": "sass src/_package.scss dist/video-embed.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:njk": "mkdir -p dist && cp src/_video-embed.njk dist/video-embed.njk || true",
    "build": "npm run build:css && npm run build:njk",
    "prepare": "npm run build"
  },
  "keywords": ["wmca","design-system","video-embed","css","nunjucks"],
  "author": "WMCA",
  "license": "MIT",
  "publishConfig": {"access":"public"},
  "devDependencies": {"sass":"^1.32.0"}
}
