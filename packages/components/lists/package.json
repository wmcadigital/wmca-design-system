{
  "name": "@wmcadigital/components/lists",
  "version": "0.1.0",
  "description": "Lists component collection (ordered, unordered, bare) from WMCA design system",
  "main": "dist/lists.min.css",
  "style": "dist/lists.min.css",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build:css": "sass src/_package.scss dist/lists.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:njk": "mkdir -p dist && cp src/*/_*.njk dist/ || true",
    "build": "npm run build:css && npm run build:njk",
    "prepare": "npm run build"
  },
  "keywords": ["wmca", "design-system", "lists", "css", "nunjucks"],
  "author": "WMCA",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "sass": "^1.32.0"
  }
}
