{
  "name": "@wmcadigital/components/loader",
  "version": "0.1.0",
  "description": "Loader component (CSS and Nunjucks) from WMCA design system",
  "main": "dist/loader.min.css",
  "style": "dist/loader.min.css",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build:css": "sass src/_package.scss dist/loader.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:njk": "mkdir -p dist && cp src/_loader.njk dist/loader.njk || true",
    "build": "npm run build:css && npm run build:njk",
    "prepare": "npm run build"
  },
  "keywords": ["wmca", "design-system", "loader", "css", "nunjucks"],
  "author": "WMCA",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "sass": "^1.32.0"
  }
}
