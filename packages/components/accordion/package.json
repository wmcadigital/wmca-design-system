{
  "name": "@wmcadigital/components/accordion",
  "version": "1.0.0",
  "description": "Accordion component styles and JS from WMCA design system",
  "main": "dist/accordion.min.css",
  "style": "dist/accordion.min.css",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build:css": "sass src/_package.scss dist/accordion.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:js": "mkdir -p dist && cp src/accordion.js dist/accordion.js || true",
    "build": "npm run build:css && npm run build:js",
    "prepare": "npm run build"
  },
  "keywords": ["wmca", "design-system", "accordion", "css", "js"],
  "author": "WMCA",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "public"
  }
}
