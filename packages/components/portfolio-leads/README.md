# @wmcads/portfolio-leads

Standalone package for the Portfolio Leads component. Compiles CSS and provides a Nunjucks macro.

Build

```
npm run build
```

Output

- `dist/portfolio-leads.min.css` — compressed CSS
- `dist/portfolio-leads.njk` — Nunjucks macro
