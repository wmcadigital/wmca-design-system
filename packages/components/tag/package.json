{
  "name": "@wmcadigital/components/tag",
  "version": "0.1.0",
  "description": "Tag component (SCSS and Nunjucks) from WMCA design system",
  "main": "dist/tag.min.css",
  "style": "dist/tag.min.css",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build:css": "sass src/_package.scss dist/tag.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:njk": "mkdir -p dist && cp src/_tag.njk dist/tag.njk || true",
    "build": "npm run build:css && npm run build:njk",
    "prepare": "npm run build"
  },
  "keywords": ["wmca","design-system","tag","css","nunjucks"],
  "author": "WMCA",
  "license": "MIT",
  "publishConfig": {"access":"public"},
  "devDependencies": {"sass":"^1.32.0"}
}
