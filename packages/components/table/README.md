# @wmcads/table

Standalone package for the Table component. Compiles CSS and provides a Nunjucks macro.

Build

```
npm run build
```

Output

- `dist/table.min.css` — compressed CSS
- `dist/table.njk` — Nunjucks macro
