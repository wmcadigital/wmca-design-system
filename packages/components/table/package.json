{
  "name": "@wmcadigital/components/table",
  "version": "0.1.0",
  "description": "Table component (SCSS and Nunjucks) from WMCA design system",
  "main": "dist/table.min.css",
  "style": "dist/table.min.css",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build:css": "sass src/_package.scss dist/table.min.css --no-source-map --style=compressed --load-path=../../src/wmcads/assets/sass/shared --load-path=../../src/wmcads/assets/sass/wmca",
    "build:njk": "mkdir -p dist && cp src/_table.njk dist/table.njk || true",
    "build": "npm run build:css && npm run build:njk",
    "prepare": "npm run build"
  },
  "keywords": ["wmca","design-system","table","css","nunjucks"],
  "author": "WMCA",
  "license": "MIT",
  "publishConfig": {"access":"public"},
  "devDependencies": {"sass":"^1.32.0"}
}
