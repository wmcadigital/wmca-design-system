{% macro wmcadsContentCardInlineNavigation(params) %}
{# imports #}
{% from "wmcads/components/content-card/_content-card.njk" import wmcadsContentCard %}
{% from "wmcads/components/inset-text/_inset-text.njk" import wmcadsInsetText %}
{# Set #}
{% set title = "Page contents" if params.withCurrentPage else "Related content" %}
{# Set up nav links #}
{% set navLinks = ["Information for drivers", "Information for bus passengers", "Information for tram passengers", "Information for pedestrians", "Getting  to venues and attractions"] %}

{% call wmcadsContentCard() %}
    <nav class="wmcads-p-sm">
        <h3 class="wmcads-content-card__title">{{title}}</h3>
        <ul class="wmcads-content-card__list wmcads-m-b-none">
            {#- Current page -#}
            {% if params.withCurrentPage %}
            <li class="wmcads-content-card__list-item">
                {{
                    wmcadsInsetText({
                        html: "
    <span aria-current=\"page\">
        Overview
    </span>
                        "
                    }) | indent(14) | trim
                }}
            </li>
            {% endif -%}
            {#- Loop through links  -#}
            {% for link in navLinks %}
            <li class="wmcads-content-card__list-item">
                <a href="#" target="_self" title="Link title" class="wmcads-link">{{link}}</a>
            </li>
            {% endfor %}
        </ul>
    </nav>
{% endcall %}

{% endmacro %}
